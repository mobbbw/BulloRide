<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Admin</title>

    <link rel="stylesheet" href="/style.css">
</head>
<body class="body">
    <div class="form-container">
        <h2>Admin</h2>
        <form action="/admin" method="post">
            <div class="templet">
               <% for(let i=0; i<Udata.length; i++){%>
                <div class="bo">
                    <div class="form-group">
                        <label for="name">Email:<%= Udata[i] %></label>
                    </div>
                    <div class="form-group">
                        <label for="password" id="password" >password:<%= Upass[i] %></label>  
                        <button type="submit"name="bt" value="<%= i %>" >delete</button>
                    </div>
                    
                </div>
                <% }%>
         </div>
            
        </form>
      
        
    </div>
</body>
</html>